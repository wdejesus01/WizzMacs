#+TITLE:Welcome to the wonderful world of wizardry
#+PROPERTY: header-args:emacs-lisp :tangle ./init.el
* General Setting
** User-Settings
:PROPERTIES:
:ID:       174C9835-6E89-4584-BF21-D2C94B524B67
:END:
#+begin_src emacs-lisp
  (setq inhibit-startup-message t); Removes starting page

  (global-display-line-numbers-mode 1);Activates number lines
  (setq display-line-numbers-type 'relative);Number lines are relative to the current line


  (setq history-length 20) ;Sets the amount of recent files tracked

  ;; Moves custom variables to a seprate file and loads it
  ;; custom-file determines where custom variables are stored
  ;; locate-user-emacs-file Resolves path within the init.el directory
  (setq custom-file (locate-user-emacs-file "custom-vars.el"));

  ;; Loads the custom file
  ;; Don't throw errors or put messages in minibuffer if errors occurs
  (load custom-file 'noerror 'nomessage)

  (setq use-dialog-box nil);Turns off graphical dialog box(less mouse clickey)

  (setq global-auto-revert-non-file-buffers t);;Same as above but for all buffers

  (fset 'yes-or-no-p 'y-or-n-p);Sets yes or no to y or no
  ;(add-to-list 'image-types 'svg) ; Fixed inavlid type svg for macos

  (add-to-list 'default-frame-alist '(undecorated . t))
#+end_src

#+RESULTS:
: ((undecorated . t) (vertical-scroll-bars))

** Global Minor modes
:PROPERTIES:
:ID:       FEBDA81A-5682-4AE0-8751-E5ECD09C39EB
:END:
#+begin_src emacs-lisp
  (global-auto-revert-mode 1);If file has changed, auto loads changes
  (recentf-mode 1); loads recent files that you have edited
  (scroll-bar-mode -1)
  (menu-bar-mode 1)
  (tool-bar-mode -1) ; if tool bar shows up
  (save-place-mode 1) ; Saves and restores last location of file
#+end_src
** Buffers
:PROPERTIES:
:ID:       F6B32097-82B8-404C-B86B-FA3933B524FB
:END:
#+begin_src emacs-lisp
  (keymap-global-set "C-x C-b" 'buffer-menu)
  (add-to-list 'global-auto-revert-ignore-modes 'Buffer-menu-mode)
#+end_src

#+RESULTS:
| Buffer-menu-mode |
** Registers
:PROPERTIES:
:ID:       78BC0D01-4F7F-4AC4-8A43-490D042AF5EA
:END:
#+begin_src emacs-lisp
  (set-register ?h '(file . "~/"))
  (set-register ?e '(file . "~/.emacs.d/"))
  (set-register ?i '(file . "~/.emacs.d/WizzyMacs.org"))
  (set-register ?o '(file . "~/org/"))
  (set-register ?s '(file . "~/org/College/"))
#+end_src

#+RESULTS:
: (file . ~/org/College/)
*** Bookmarks 
:PROPERTIES:
:ID:       15924138-BC88-4206-81A4-936D45A7C2A3
:END:
#+begin_src emacs-lisp
  (setq bookmark-save-flag 1)
#+end_src

#+RESULTS:
: 1

** Find Sudo-File
:PROPERTIES:
:ID:       C3578062-D5A5-4120-882D-9C17DF95139C
:END:
#+begin_src emacs-lisp
(defun sudo-find-file (file-name)
  "Like find file, but opens the file as root."
  (interactive "FSudo Find File: ")
  (let ((tramp-file-name (concat "/sudo::" (expand-file-name file-name))))
    (find-file tramp-file-name)))
#+end_src
** Info
:PROPERTIES:
:ID:       6FF29C05-94DC-4A76-8507-4582AFFF1DE7
:END:
:NOTES:
- Note taken on [2025-10-26 Sun 13:04] \\
  Need to update such that System info path = emacs info path
:END:
#+begin_src emacs-lisp
;  (setq Info-default-directory-list (append Info-default-directory-list "~/.emacs.d/info/"
;  (string-trim (shell-command-to-string "echo $INFOPATH") "[ \t\n\r]+" "[ \t\n\r:]+")))
#+end_src

#+RESULTS:
| /opt/homebrew/share/info | ~/.emacs.d/info/ |

** Straight.el
:PROPERTIES:
:ID:       C7B290DF-E922-4CBE-BCDE-535CA8AFC80D
:END:
#+begin_src emacs-lisp
  
  (defvar bootstrap-version)
  (let ((bootstrap-file
        (expand-file-name "straight/repos/straight.el/bootstrap.el" user-emacs-directory))
        (bootstrap-version 5))
    (unless (file-exists-p bootstrap-file)
      (with-current-buffer
          (url-retrieve-synchronously
          "https://raw.githubusercontent.com/raxod502/straight.el/develop/install.el"
          'silent 'inhibit-cookies)
        (goto-char (point-max))
        (eval-print-last-sexp)))
    (load bootstrap-file nil 'nomessage))


  (straight-use-package 'use-package)
  (setq straight-use-package-by-default t)
#+end_src
** No-Littering
:PROPERTIES:
:ID:       FECDD863-3A41-41A4-ADAD-7BE4B9A914A5
:END:
#+begin_src emacs-lisp
  (use-package no-littering
  :straight t)
  (no-littering-theme-backups)
#+end_src
** exec-path-from-shell
:PROPERTIES:
:ID:       FBCD02AB-5B82-4FE7-A7EC-AD997AB8A9DD
:END:
#+begin_src emacs-lisp
  (use-package exec-path-from-shell
    :straight t
    :config
    (add-to-list 'exec-path-from-shell-variables "INFOPATH") 
    (when (memq window-system '(mac ns x))
      (exec-path-from-shell-initialize)))
#+end_src

#+RESULTS:
: t

** Window Management
*** Tabs 
:PROPERTIES:
:ID:       724A71D0-FEC8-46B3-B43E-28717126B765
:END:
#+begin_src emacs-lisp
  (setq tab-bar-show 1)
#+end_src

#+RESULTS:
: 1

*** Frames
:PROPERTIES:
:ID:       1A24C669-F361-4663-B4AF-50982363DADA
:END:
#+begin_src emacs-lisp
  (keymap-global-set "C-x 5 w" 'set-frame-width)
  (keymap-global-set "C-x 5 h" 'set-frame-height)
#+end_src
*** Fullscreen
:PROPERTIES:
:ID:       55708166-1CED-4452-91D8-BDB396515D76
:END:
#+begin_src emacs-lisp
  (define-key global-map (kbd "<f1>") #'toggle-frame-fullscreen)
#+end_src
** minibuffer 
:PROPERTIES:
:ID:       C5366834-FAD6-4BED-8571-FDFB5FF42CED
:END:
#+begin_src emacs-lisp
  ;; Appeneded Oct/15/25 6:08PM
  (minibuffer-electric-default-mode) ; Alters default arguement behaviour in minibuffer
#+end_src

* Stuff
:PROPERTIES:
:ID:       29E66BE7-9845-4A8C-BCD1-59261923690D
:END:
#+begin_src emacs-lisp
  (use-package straight
  :custom
  ;; add project and flymake to the pseudo-packages variable so straight.el doesn't download a separate version than what eglot downloads.
  (straight-built-in-pseudo-packages '(emacs nadvice python image-mode project flymake))
  (straight-use-package-by-default t))
#+end_src

#+RESULTS:

* Evil
:PROPERTIES:
:ID:       715852F7-5EC4-4E5C-B853-BD017FA95E1D
:END:
#+begin_src emacs-lisp
  (use-package evil
    :straight t
    :custom
    (evil-want-minibuffer t)
    :init
    (setq evil-want-integration t
  	  evil-want-keybinding nil
  	  evil-want-fine-undo t
  	  evil-want-C-w-in-emacs-state t)
    :config
    (evil-set-undo-system 'undo-redo)
    (keymap-set evil-insert-state-map "C-c" 'evil-normal-state)
    (keymap-unset evil-motion-state-map "<SPC>" t) 
    (evil-mode 1))

#+end_src

 #+RESULTS:
 : t
** Evil-Collection
:PROPERTIES:
:ID:       60166E67-C096-481E-9034-9BC1D1199E99
:END:
#+begin_src emacs-lisp
  (use-package evil-collection
  :after evil
  :straight t
  :config
  (evil-collection-init))
#+end_src

#+RESULTS:
: t
* Completion ** Vertico :PROPERTIES: :ID:       4E6C1458-9F1A-4EDE-A55E-1E3E2BC90F07 :END: #+begin_src emacs-lisp (use-package vertico :straight t :config (vertico-mode 1)) #+end_src #+RESULTS: : t ** Marginalia :PROPERTIES: :ID:       755B8FC6-B576-4B24-809F-4A0025A1BF26 :END: #+begin_src emacs-lisp (use-package marginalia :after vertico :straight t :config (marginalia-mode 1)) #+end_src #+RESULTS: : t ** Savehist :PROPERTIES: :ID:       E535245B-DD32-4058-8876-54B6D2B1A947 :END: #+begin_src emacs-lisp (use-package savehist :config (savehist-mode)) #+end_src #+RESULTS: : t ** Which-Key :PROPERTIES: :ID:       B53ECA0E-FC46-44C2-9ED7-A9E4CE5380FF :END: #+begin_src emacs-lisp (use-package which-key :straight t :config (which-key-mode)) #+end_src ** Orderless :PROPERTIES: :ID:       79EAD23D-085F-4304-A29A-FD63131D659D :END: #+begin_src emacs-lisp (use-package orderless :straight t :custom (completion-styles '(orderless basic)) (completion-category-overrides '((file (styles basic partial-completion))))) #+end_src #+RESULTS: ** Flycheck :PROPERTIES: :ID:       14688A7E-F968-4CA3-BED3-73CD4210DA79 :END: #+begin_src emacs-lisp (use-package flycheck :straight t) (global-flycheck-mode) #+end_src ** Company :PROPERTIES: :ID:       51274AA8-C7AC-44BE-9722-D26AA360BD99 :END: #+begin_src emacs-lisp (use-package company :config (setq company-idle-delay 0 company-minimum-prefix-length 1 company-tooltip-align-annotations t)) (add-hook 'after-init-hook 'global-company-mode) #+end_src #+RESULTS: | global-company-mode | org-persist-load-all | tramp-register-archive-autoload-file-name-handler | table--make-cell-map |
* Ispell
:PROPERTIES:
:ID:       25921EFB-FFA1-479B-9FDA-EB769911320F
:END:
#+begin_src emacs-lisp
  ;;; Added 10/09/25 6:50AM
  ;;; Set ispell program to hunspell
  (setq ispell-program-name (string-trim (shell-command-to-string "which hunspell")))
#+end_src

#+RESULTS:
: /opt/homebrew/bin/hunspell

* Org
:PROPERTIES:
:ID:       84AC8A6E-8ECE-48E2-AC8B-E0311CDF4AAF
:END:
#+begin_src emacs-lisp
  (use-package org
    :defer
    :straight '(org
  	      :fork (:host nil
                       :repo "https://git.tecosaur.net/tec/org-mode.git"
                       :branch "dev"
                       :remote "tecosaur")
                :files (:defaults "etc")
                :build t
                :pre-build
                (with-temp-file "org-version.el"
  		(require 'lisp-mnt)
                 (let ((version
                        (with-temp-buffer
                          (insert-file-contents "lisp/org.el")
                          (lm-header "version")))
                       (git-version
                        (string-trim
                         (with-temp-buffer
                           (call-process "git" nil t nil "rev-parse" "--short" "HEAD")
                           (buffer-string)))))
                  (insert
                   (format "(defun org-release () \"The release version of Org.\" %S)\n" version)
                   (format "(defun org-git-version () \"The truncate git commit hash of Org mode.\" %S)\n" git-version)
                   "(provide 'org-version)\n")))
                :pin nil) 
    :init (require 'ox-md)
    :bind (:map org-mode-map
  	      ("C-c C-|" . org-table-insert-column)
  	      ("C-c C-x i" . org-id-get-create))
    :hook (org-mode . flyspell-mode)
    :custom
    (org-directory "~/org/agenda")
    (org-id-link-to-org-use-id  t)
    (org-attach-method 'lns)
    (org-attach-id-dir "refs")
    (org-archive-location "archive/%s_archive::")
    :config
     (setq org-html-validation-link nil org-hide-emphasis-markers t
  	   org-list-allow-alphabetical t)
     (keymap-global-set "C-c l" 'org-store-link)
     (keymap-global-set "C-c a" 'org-agenda)
     (keymap-global-set "C-c c" 'org-capture))

#+end_src
#+RESULTS:
: org-id-get-create
** org-latex-preview
:PROPERTIES:
:ID:       B13123CC-8EB3-45FA-9CE3-45ECDBA1B6F0
:END:
#+begin_src emacs-lisp
 ;; Increase preview width
  (plist-put org-latex-preview-appearance-options
             :page-width 0.8)

  ;; ;; Use dvisvgm to generate previews
  ;; ;; You don't need this, it's the default:
  ;; (setq org-latex-preview-process-default 'dvisvgm)
  
  ;; Turn on `org-latex-preview-mode', it's built into Org and much faster/more
  ;; featured than org-fragtog. (Remember to turn off/uninstall org-fragtog.)
  (add-hook 'org-mode-hook 'org-latex-preview-mode)

  ;; ;; Block C-n, C-p etc from opening up previews when using `org-latex-preview-mode'
  ;; (setq org-latex-preview-mode-ignored-commands
  ;;       '(next-line previous-line mwheel-scroll
  ;;         scroll-up-command scroll-down-command))

  ;; ;; Enable consistent equation numbering
  ;; (setq org-latex-preview-numbered t)

  ;; Bonus: Turn on live previews.  This shows you a live preview of a LaTeX
  ;; fragment and updates the preview in real-time as you edit it.
  ;; To preview only environments, set it to '(block edit-special) instead
  (setq org-latex-preview-mode-display-live t)

  ;; More immediate live-previews -- the default delay is 1 second
  (setq org-latex-preview-mode-update-delay 0.25)
#+end_src

#+RESULTS:
: 0.25
*** org-latex-preview-center
:PROPERTIES:
:ID:       3F3ED5BD-A254-4F02-A5C6-122084640B18
:END:
#+begin_src emacs-lisp
(defun my/org-latex-preview-uncenter (ov)
    (overlay-put ov 'before-string nil))
  (defun my/org-latex-preview-recenter (ov)
    (overlay-put ov 'before-string (overlay-get ov 'justify)))
  (defun my/org-latex-preview-center (ov)
    (save-excursion
      (goto-char (overlay-start ov))
      (when-let* ((elem (org-element-context))
                  ((or (eq (org-element-type elem) 'latex-environment)
                       (string-match-p "^\\\\\\[" (org-element-property :value elem))))
                  (img (overlay-get ov 'display))
                  (prop `(space :align-to (- center (0.55 . ,img))))
                  (justify (propertize " " 'display prop 'face 'default)))
        (overlay-put ov 'justify justify)
        (overlay-put ov 'before-string (overlay-get ov 'justify)))))
  (define-minor-mode org-latex-preview-center-mode
    "Center equations previewed with `org-latex-preview'."
    :global nil
    (if org-latex-preview-center-mode
        (progn
          (add-hook 'org-latex-preview-overlay-open-functions
                    #'my/org-latex-preview-uncenter nil :local)
          (add-hook 'org-latex-preview-overlay-close-functions
                    #'my/org-latex-preview-recenter nil :local)
          (add-hook 'org-latex-preview-overlay-update-functions
                    #'my/org-latex-preview-center nil :local))
      (remove-hook 'org-latex-preview-overlay-close-functions
                    #'my/org-latex-preview-recenter)
      (remove-hook 'org-latex-preview-overlay-update-functions
                    #'my/org-latex-preview-center)
      (remove-hook 'org-latex-preview-overlay-open-functions
                    #'my/org-latex-preview-uncenter)))
#+end_src

#+RESULTS:

** Keys
:PROPERTIES:
:ID:       5D383210-D8F1-4239-AACA-645AAEA68CF6
:END:
#+begin_src emacs-lisp
  (evil-define-key 'normal org-mode-map
    (kbd "SPC h") #'org-insert-heading
    (kbd "SPC a h") #'org-insert-heading-after-current
    (kbd "SPC s h") #'org-insert-subheading)
#+end_src

#+RESULTS:

** todo-keywords
:PROPERTIES:
:ID:       E16BD9C2-B760-4FB7-B803-374A06CDAED3
:END:
#+begin_src emacs-lisp
  (setq org-todo-keywords
        '((sequence
  	 "TODO(T)" 
  	 "BUY((B)"
  	 "HOLD(H)" 
  	 "?(?)" ;To be Considered
  	 "|"
  	 "DONE(D)"
  	 "VOID(V)" 
  	)
  	;;; Damn you english phonetics
  	(sequence
  	 "TOREAD(r)"
  	 "READING"
  	 "|"
  	 "READ(R)"
  	 )
  	(sequence
  	 "LISTEN(l)"
  	 "LISTENED(L)"))

        org-tag-alist ;Controlled Vocabulary of tags
        '(("emacs" . ?e);Prima Facie(Self-evident)
  	("info" . ?i) ;Information about Information
  	("cmpt" . ?c) ;Computers & their sciences
  	)
        org-fast-tag-selection-single-key t) ;C-u once to show selection,twice to remove single-key-exit
        

  	 

#+end_src

#+RESULTS:
: t
** Org-Tempo
:PROPERTIES:
:ID:       FD73FAD8-8D68-48AC-8E35-EFF2DCE85C9F
:END:
#+BEGIN_SRC emacs-lisp
  (use-package org-tempo
  :straight '(:type built-in)
  :config
  (dolist (template '(("el" .  "src emacs-lisp")
  		    ("x" .  "src C")
  		    ("lp" . "src lisp"))) 
    (add-to-list 'org-structure-template-alist template)))
#+END_SRC

#+RESULTS:
: t

** org-goto
:PROPERTIES:
:ID:       4E040161-331A-4955-9F0A-16A1AF7995C4
:END:
#+begin_src emacs-lisp
  (setq org-goto-interface 'outline-path-completion)
#+end_src

#+RESULTS:
: outline-path-completion

** org-refile
:PROPERTIES:
:ID:       C878BDF2-B1E8-45EE-80F3-C6B1ED4F6199
:END:
[[https://blog.aaronbieber.com/2017/03/19/organizing-notes-with-refile.html][Inspiration]]
#+begin_src emacs-lisp
  (setq org-refile-allow-creating-parent-nodes 'confirm
        org-refile-use-outline-path 'file
        org-outline-path-complete-in-steps nil
        org-refile-targets '((org-agenda-files   :maxlevel . 2)
  			   (nil :maxlevel . 3 )))

#+end_src

#+RESULTS:
: ((org-agenda-files :maxlevel . 2) (nil :maxlevel . 3))

** Org-Babel
*** Languages
:PROPERTIES:
:ID:       915EF143-DCCB-4523-A984-4E71CDEF6961
:END:
#+BEGIN_SRC emacs-lisp
      ;;select languages for bable
    (org-babel-do-load-languages
     'org-babel-load-languages
     '((emacs-lisp . t)
       (C . t)
       (python . t)
       (shell . t)
       (scheme . t)
       (lisp . t)))

    
  (setq org-babel-python-command "python3")
  (setq org-confirm-babel-evaluate nil);;Confirmation to execute code block


#+END_SRC 

#+RESULTS:

*** Tangle
:PROPERTIES:
:ID:       FC607CCA-D32B-466B-BA4E-0B56847DAF6D
:END:
#+BEGIN_SRC emacs-lisp
  (defun efs/org-babel-tangle-config()
  (when(string-equal (buffer-file-name)
                     (expand-file-name "~/.emacs.d/WizzyMacs.org"))

  (let ((org-confirm-babel-evaluate nil))
    (org-babel-tangle))))

  (add-hook 'org-mode-hook (lambda () (add-hook 'after-save-hook #'efs/org-babel-tangle-config)))

 #+END_SRC

#+RESULTS:
| #[0 \301\211\207 [imenu-create-index-function org-imenu-get-tree] 2] | (lambda nil (add-hook 'after-save-hook #'efs/org-babel-tangle-config)) | org-tempo-setup | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-fold-show-all append local] 5] | #[0 \300\301\302\303\304$\207 [add-hook change-major-mode-hook org-babel-show-result-all append local] 5] | org-babel-result-hide-spec | org-babel-hide-all-hashes | flyspell-mode |

** Org-Capture
:PROPERTIES:
:ID:       71885157-9842-4CC6-A052-57804E3D8428
:END:
- For help with templates go [[info:org#Capture templates][here]] 
#+begin_src emacs-lisp
  (setq org-default-notes-file (expand-file-name "log.org" org-directory)
        org-capture-templates
  	'(("c" "Capture" entry (file "") 
  	   "* ?  %?\n")
  	  ;Made at 2:22pm Oct 2nd
  	  ;Template to create entry for a class using org-goto to choose headline
  	  ("s" "School" entry (file+function "school.org"
  					     (lambda () (let ((org-goto-max-level  2)))
  					       (org-goto)))
  	   "* ?  %?\n")))
  							   
  	   
#+end_src

#+RESULTS:
| c | Capture | entry | (file )                                                                           | * ?  %? |
| s | School  | entry | (file+function school.org (lambda nil (let ((org-goto-max-level 2))) (org-goto))) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org (lambda nil (let ((org-goto-max-level 2))) (org-goto))) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org (lambda nil (let ((org-goto-max-level 2))) (org-goto))) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org (lambda nil (let ((org-goto-max-level 2))) (org-goto))) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org (lambda nil (let ((org-goto-max-level 2))) (org-goto))) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org org-goto) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org org-goto) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| s | School | entry | (file+function school.org (org-goto)) | * ?  %? |

Captured on: %U |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |
| c  | Computer Science         |       |                                                                                     |
| cl | Linux Kernel Programming | plain | #'(lambda nil (shell-command (format touch %s (read-file-name Class: ~/College/)))) |

Captured on: %U |
| e | Emacs | entry | (file emacs.org) | * ?  %? |

Captured on: %U |

** Appearence
*** org-Settings
:PROPERTIES:
:ID:       2F5483AF-0E96-4A2A-83E0-38076C2F347B
:END:
#+BEGIN_SRC emacs-lisp
  (setq org-hidden-keywords '(title)
        org-startup-indented t
        org-startup-with-inline-images t
        org-startup-folded t)
#+END_SRC

#+RESULTS:
*** Org-Bullets
:PROPERTIES:
:ID:       4BF2CB1D-DA41-4DB6-95FA-9F59248E5259
:END:
#+begin_src emacs-lisp
  (use-package org-bullets
    :straight t
    :config
    (add-hook 'org-mode-hook (lambda () (org-bullets-mode t))))
#+end_src

#+RESULTS:
: t
* UI
** Themes
*** Tao
:PROPERTIES:
:ID:       0EE4712C-1AD7-4BE6-A016-396E6D3679C9
:END:
#+begin_src emacs-lisp
  (use-package tao-theme
    :straight t)
#+end_src
*** Spacemacs
:PROPERTIES:
:ID:       40B9C9AF-C998-4C6D-8EDE-F3BD203F1F3E
:END:
#+begin_src emacs-lisp
  (use-package spacemacs-theme
    :straight t)
#+end_src

#+RESULTS:

*** Theme Selection
:PROPERTIES:
:ID:       F67D48A2-A18F-49D0-A4B5-007952F07855
:END:
#+begin_src emacs-lisp
(load-theme 'spacemacs-dark) 
#+end_src
** Text 
:PROPERTIES:
:ID:       33BB2C5E-EDE6-4F13-BD97-739DF7FC2E84
:END:
#+begin_src emacs-lisp
  (set-face-attribute 'default nil :height 150)
  (set-frame-font "JetBrains Mono" nil t)
  (global-hl-line-mode -1) 
  (hl-line-mode -1)
#+end_src
* Mail
:PROPERTIES:
:ID:       F227300E-0441-4C08-AD01-98933272227D
:END:
** browse-url-mailto
:PROPERTIES:
:ID:       AA62AFCC-EA69-45F8-9F42-A6AC874C6D43
:END:
#+begin_src emacs-lisp
  (setq browse-url-mailto-function 'browse-url-generic)
  (setq browse-url-generic-program "open")
#+end_src

#+RESULTS:
: open

* Programming
** LISP  
*** Geiser
:PROPERTIES:
:ID:       50059CE5-3014-4625-A097-6831F68DECCB
:END:
#+begin_src emacs-lisp
  (use-package geiser
    :straight t
    :custom
    (geiser-active-implementations '(racket)))

  (use-package geiser-racket
    :straight t)
#+end_src

#+RESULTS:
*** SLIME
:PROPERTIES:
:ID:       C35698EC-8BB5-4E5B-8029-F60356BCDB98
:END:
#+begin_src emacs-lisp
  (use-package slime
    :straight t
    :config 
    (setq inferior-lisp-program "sbcl"))
#+end_src

#+RESULTS:
** Eglot
:PROPERTIES:
:ID:       E9986D3F-1403-4ACA-8831-159BFC28B229
:END:
#+begin_src emacs-lisp
  (use-package eglot
    :straight (:type built-in)
    :hook ((python-mode . eglot-ensure)))
#+end_src

#+RESULTS:
| eglot-ensure | eglot | evil-collection-python-set-evil-shift-width |
** sql-indent
:PROPERTIES:
:ID:       8A3EC1DD-7AF5-4307-B5CD-C757641AFED1
:END:
#+begin_src emacs-lisp
  (use-package sql-indent
    :straight t
    :hook (sql-mode . sqlind-minor-mode))
#+end_src

#+RESULTS:
| sqlind-minor-mode | sql-indent-mode | sql-indent-enable |
